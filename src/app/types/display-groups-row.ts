import type { DisplayGroup } from './display-group.js';

// DisplayGroupsRow: Runtime type for a horizontal row of Display Groups
export interface DisplayGroupsRow {
  displayGroups: DisplayGroup[];
}
